// Re-export shim. The canonical home for `Type` and friends is
// `primitives/Type`; this file keeps existing relative imports working.
export { Type, TypeBlock, TypeBox, SignatureExpr } from '../primitives/Type.js'
