export { Route, useParams, Navigate, HashRouter, useLocation, useNavigate, A } from '@solidjs/router'
// import { A as SA } from '@solidjs/router'
// import { isServer } from 'solid-js/web'

// export const A: typeof SA = (props) => {
//   if (isServer) return <a href={props.href}>{props.children}</a>
//   return <SA {...props} />
// }
